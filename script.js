let a = parseInt(prompt('Digite um número inteiro.'));
let b = parseInt(prompt('digite outro número inteiro.'));

// faz somente a multiplicação.

function calcular(a, b){
    return a*b;

      
}

calcular(
    alert(`o resultado é ${a*b}`)
);

console.log(a*b);

